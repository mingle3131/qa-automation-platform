[pytest]
# 테스트 파일을 찾을 경로
testpaths = tests

# 테스트 파일 이름 패턴 (test_ 로 시작하는 파일만 인식)
python_files = test_*.py

# 테스트 함수 이름 패턴 (test_ 로 시작하는 함수만 인식)
python_classes = Test*
python_functions = test_*

# 테스트 실행 시 상세 출력
addopts = -v